dist
dist-site/
dist-h5/
dist-app/

node_modules
.pnpm
.npm
.dev
.node
.workbuddy-ai
.workbuddy-ai

# 运行时数据：SQLite 数据库及 WAL 文件（用 npm run seed 重新生成）
server/data/*.db
server/data/*.db-wal
server/data/*.db-shm

# 编辑器 / 系统
.DS_Store
*.log
.vite/

pnpm-lock.yaml

# Vite 生成配置时的临时文件
vite.config.js.timestamp-*.mjs
